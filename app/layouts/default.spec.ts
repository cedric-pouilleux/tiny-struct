import { describe, it } from 'vitest'

describe('Default layout', () => {
  it('Should not render logged item is not logger', () => {
    // todo
  })

  it('Should render loggin item without session', () => {
    // todo
  })
})
